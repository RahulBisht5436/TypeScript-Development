const {studentArray} = require("./addtional Files/variable.js")
const {SelectionStatement,switchStatmentFunction ,repeatingConsole} = require("./addtional Files/flowControl.js")
console.log(`This is the new log: ${new Date().toLocaleString("en-IN", {
    timeZone: "Asia/Kolkata"
})} =========================>>>>>`);

SelectionStatement(2)
switchStatmentFunction(1)
repeatingConsole(10)

// console.log(studentArray)
